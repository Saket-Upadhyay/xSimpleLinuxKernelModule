Use "make" to build module,

Use "sudo cat /proc/kmsg" to view kernel messages.

Use "sudo insmod xBasicKM.ko" to load the module.

Use "sudo rmmod xBasicKM" to remove the module.

You should see new entries in the log as you perform last two steps.


